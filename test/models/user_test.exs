defmodule PlanningPoker.UserTest do
  # use PlanningPoker.ModelCase
  #
  # alias PlanningPoker.User
  #
  # @valid_attrs %{name: "My Name", auth_token: "token"}
  # @without_auth_token %{cards: []}
  #
  # test "changeset with valid attributes" do
  #   changeset = User.changeset(%User{}, @valid_attrs)
  #   assert changeset.valid?
  # end
  #
  # test "changeset wihout name" do
  #   changeset = User.changeset(%User{}, @without_auth_token)
  #   refute changeset.valid?
  # end
end

